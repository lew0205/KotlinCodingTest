package BaekJoon.IO._10926_놀람이모티콘

import java.io.*
import java.lang.StringBuilder

fun main() = with(BufferedReader(InputStreamReader(System.`in`))) {

    val sb = StringBuilder()
    sb.append(readLine() + "??!")

    print(sb)
}